server:
  port: 80

logging:
  level:
    org.hibernate.engine.jdbc.env.internal.LobCreatorBuilderImpl: ERROR

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    platform: postgres
    url: jdbc:postgresql://catalyte-reservation-db.cjqak8yeav1x.us-east-1.rds.amazonaws.com
    username: postgres
    password: uNuSNtE9QpxpepeS
    driverClassName: org.postgresql.Driver